# Reference knowledge assets rather than embed them

Skills reference Knowledge Assets instead of bundling large knowledge corpora directly. This keeps Skill artifacts smaller and easier to version, while allowing knowledge sources to evolve independently and remain usable in offline deployments through separately managed asset sync.
