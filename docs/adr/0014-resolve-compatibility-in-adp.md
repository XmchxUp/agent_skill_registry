# Resolve compatibility in ADP

Skill compatibility is resolved when ADP publishes an Agent Profile, using each Skill's declared Compatibility Contract. Customer-environment Controllers verify the resolved contract and reject incompatible workloads rather than trying to adapt them locally, which keeps offline runtime behavior deterministic and supportable.
